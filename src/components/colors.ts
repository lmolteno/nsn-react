import { blue } from '@mui/material/colors'

export const colors = {
    table: {
        lightRow: '#fffff',
        darkRow: '#f5f5f5',
        headColor: '#000',
        headText: '#fff'
    },
    lightCheckbox: blue[50]
}